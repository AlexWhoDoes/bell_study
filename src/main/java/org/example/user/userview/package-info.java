/**
 * Package is intended to provide response UserView objects
 * contains UserView, it is a full version contained all of fields needed to form responses according to api GET request
 * UserViewShort, it is a short version contained some of fields, needed to form responses according to api/list POST request
 */
package org.example.user.userview;